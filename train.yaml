apiVersion: v1
kind: Pod
metadata:
  generateName: seq49-batchlim10-50epochs-noskip-
  labels:
    type: interactive
    research-group: rl-team
spec:
  hostIPC: true
  restartPolicy: Never
  containers:
  - name: nri-train
    command: ["python", "-u", "train.py", 
    "--name=seq49_batchlim10_50epochs_noskip",
    "--epochs=50",
    "--batch_size=12",
    "--batch_limit=10",
#     "--hidden_size=256",
    "--node_embedding_dim=36",
#     "--predicted_timesteps=10",
#     "--edge_embedding_dim=2", # this is k, i.e. number of edge types
    "--seq_len=49",
#     "--skip_connection",
    ]
    # command: ["sleep", "infinity"]
    image: amr-registry.caas.intel.com/aipg/mpettee-pytorch-geometric
    workingDir: /workspace/choreo-graph/
    resources:
      limits:
        nvidia.com/gpu: 1
        cpu: 11
        memory: 30Gi
    volumeMounts:
    - mountPath: "/workspace"
      name: workspace 
    envFrom:
    - configMapRef:
        name: proxy-config
    env:
    - name: LD_LIBRARY_PATH
      value: $LD_LIBRARY_PATH:/.mujoco/mjpro150/bin
    - name: OMP_NUM_THREADS
      value: "1"
  securityContext:
    fsGroup: 17685
    runAsUser: 11964579
  volumes:
  - name: workspace
    persistentVolumeClaim:
      claimName: workdisk

